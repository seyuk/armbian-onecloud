name: gg
on:
  workflow_dispatch:
  push:
    branches:
      - readme
env:
  SCRIPT_REPO: ${{ github.repository }}
  SCRIPT_REF: ${{ github.ref_name }}
jobs:
 test:
  name: test
  runs-on: ubuntu-22.04
  steps: 
   - name: sss
     run: |
      echo ${SCRIPT_REPO}  ${SCRIPT_REF}
